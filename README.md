# Basic_Antivirus_Simulation
Basic Antivirus Simulation - Antivirus engines detect malware by scanning files and comparing them against known signatures. that scans folders, reads file hashes, and compares them against a known malware signatures database. If a match is found, the file is flagged as malicious and optionally moved to a quarantine folder.
